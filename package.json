{
  "name": "http-server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node app.js",
    "install-app": "pnpm install && cd client && pnpm install && pnpm build",
    "dev": "nodemon app.js -- -d",
    "build": "cd client && pnpm build"
  },
  "dependencies": {
    "argparse": "^2.0.1",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "express-fileupload": "^1.2.1",
    "morgan": "^1.10.0",
    "qrcode-terminal": "^0.12.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.14",
    "semistandard": "^16.0.1"
  }
}
